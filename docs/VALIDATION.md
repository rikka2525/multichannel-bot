# 検証結果

最終確認日：2026-09-08。対象は改修版から作成した公開用コピー。元プロジェクトおよび実接続用の設定・DBは変更していません。

## 表示名変更後の最終実返信

2026-09-08 00:00（日本時間）、公開用コードのDiscord AdapterとCoreを、既存DBの検証用コピーで起動しました。元のDBは変更していません。検証用の起動補助・DB・生の記録は公開フォルダ外で管理しています。

- Discord公式APIでBotユーザー名・サーバー表示名が `MultiChannel Bot` であることを確認。
- ユーザーがテスト文を1回送信。直近メッセージを公式APIで読み取り、同じチャンネルに同Botから1返信のみであることを確認。
- 実Adapterの受信・Core処理・返信送信・DBコミット完了を実行ログで確認。
- DBの処理済み件数が6→7件、対応メッセージのハッシュが保存済み、会話状態が `survey_comment` → `menu`、送信したテスト文と保存コメントの一致を確認。
- 返信はアンケート完了のお礼とメニュー案内。実装上の順序はDB状態取得→会話処理→同じDiscordへ返信→DB更新。
- 完了後、検証用Discord接続とプロセスが正常終了したことを確認。

今回の1投稿は前回の自由記述待ち状態を継続した確認です。新しいアンケート一式や障害再現を再実施したものではありません。

## 自動テスト

Python 3.12の既存環境で`python -m unittest discover -s tests -v`を実行し、**41件成功（既存34件＋追加7件）**。外部送信はモックに置き換え、一時DBを使用しています。接続先APIやDiscord SDKの実通信を再検証したものではありません。

追加した`tests/test_failures.py`では以下を確認しました。

- Telegramの許可外ユーザー、Bot、グループ、スーパーグループはCoreにも送信APIにも渡らない。
- TelegramのHTTP 302/401/403/429/500、タイムアウト、接続エラーで状態と処理済み記録を進めない。
- Telegramの不正JSON・不正API応答を拒否し、エラーメッセージに元の例外の秘密値を含めない。
- Telegramのpollingで返信失敗した更新を、次の取得要求で確認済みにしない。
- Discordの許可外ユーザー・チャンネル・Bot投稿をイベント処理入口で拒否。
- Discordの送信権限エラーでDBを進めず、秘密の例外本文をログへ出さない。再処理成功時に受信元チャンネルへ返信し、重複処理を抑止。メンション抑制も確認。

既存テストはWhatsApp署名拒否、許可外送信者・受信用ID、重複、保存、再起動、入力ミス、送信失敗、共通Coreのサービス・会話分離などを確認しています。テスト中の`signature_rejected`や`reply_processing_failed`は意図した異常系ログです。

ローカルHTTPの`tools/smoke_test.py`も成功しました。ヘルスチェック、Webhook検証、署名付き受信、署名なし拒否を実際のループバック通信で確認し、外部には送信していません。

## 公開候補の確認

公開候補29ファイルを対象に、元のローカル設定の秘密値・許可IDとの照合、代表的なトークン・秘密鍵・メールアドレス・個人PCパスのパターン確認、ローカル文書リンク確認を実施し、検出事項はありませんでした。実DB・ログ・設定・仮想環境をコピーしていません。Git除外規則は10種類のパスで確認済みです。これは既知の秘密値とパターンによる確認で、あらゆる秘密情報の不存在を保証する監査ではありません。

既存Pythonファイルはコピー元とバイト単位で一致しています。追加したのは異常系テストと公開用文書・除外設定です。一時セットアップ補助ツール2本、古い準備履歴、重複READMEは公開用コピーに含めませんでした。元のファイルは削除していません。

## 引き継いだ実接続結果

2026-09-07の改修版検証記録に基づきます。

| サービス | 結果 |
| --- | --- |
| Discord | Gateway接続、受信→Adapter→Core→同じチャンネルへの返信、評価・コメント保存、再起動後の会話継続を確認 |
| Telegram | 個別チャットへの返信、評価5・コメント保存を確認。再起動操作後の継続はユーザー報告と提供画面に基づく。新しい評価入力で前のコメントを消す既存仕様も確認 |
| WhatsApp | 改修版では実接続未検証。元WhatsApp版の固定返信成功はこのコピーの実績に含めない |

上記の過去記録に加え、今回のDiscord実返信は冒頭の「表示名変更後の最終実返信」のとおり再確認しました。Telegramの再送信は実施していません。

## 実機での残確認

許可外ユーザー・チャンネル／グループ拒否と実通信障害は自動テストのみです。追加実機確認時はテスト用Botと架空の文面を使い、次の手順で確認します。

1. Discordの許可外ユーザー／チャンネルで投稿し、返信せずDB件数が増えないことを確認。
2. Telegramの許可外ユーザー／テストグループで同様に確認。
3. Discordのテストチャンネルの送信権限を一時的に外し、許可ユーザーが投稿。処理失敗と状態維持を確認し、権限を復元して新しい投稿で再入力。
4. Telegramはテスト環境の通信遮断時に停止することを確認し、通信復旧・再起動後の更新処理を確認。タイムアウトでは送信済みの可能性があるため重複にも注意。
5. Meta復旧後、専用設定・DBでWhatsApp送受信、評価・コメント保存、再起動継続を検証。

これらの実機結果を未確認のまま「異常系実機検証済み」「本番品質保証」とは表示しません。
