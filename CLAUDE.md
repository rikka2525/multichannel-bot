# Project Instructions

## Goal

このプロジェクトでは、安全性・保守性・テスト可能性を重視して開発する。

機能追加や修正を行うときは、まず既存コードを理解してから変更すること。

## Development Flow

原則として以下の順序で進める。

要件・禁止事項・完了条件を確認したうえで、案件フローを以下の順序で進める。

`research → implementation → test → security → review → release`

1. research：技術調査、影響範囲、依存関係、実装前の懸念を整理する。
2. implementation：調査結果を踏まえて変更方針を決め、最小限の実装・修正を行う。
3. test：必要なテストを追加し、要求動作・境界値・異常系・回帰を検証する。
4. security：入力検証、権限、秘密情報、外部API/Webhook、DBの安全性を専門にレビューする。
5. review：セキュリティ監査結果を受け取り、バグ、設計、既存機能への影響、保守性を独立してレビューする。
6. release：最終変更に対応したテスト結果、ドキュメント、PR説明、コミット候補、リリース前チェックをまとめる。

親エージェントは工程を順に依頼し、要件、変更範囲・差分、前工程の結果、未解決事項を次の担当へ渡す。
このフローは作業指示であり、自動実行する仕組みではない。
問題があれば実装修正はimplementation、テスト追加・修正はtestへ戻す。
修正後はtestで再検証し、security・reviewで影響範囲と指摘の解消を確認してからreleaseへ進む。
release中の実装修正にも同じ流れを適用し、古いテスト結果やレビュー結果だけで完了としない。

## Rules

- 既存機能を壊さない
- 必要以上に大規模な変更をしない
- 既存の設計やコーディングスタイルを優先する
- 新しい依存関係の追加は最小限にする
- APIキー、トークン、パスワードなどの秘密情報をコードへ書かない
- テスト失敗を無視しない
- テストを通すためだけのハードコードをしない
- 不要な一時ファイルは削除する
- 変更理由を説明できる状態にする

## Agent Usage

### research

実装前の調査と方針提案を担当する。コードを変更せず、根拠、影響範囲、依存関係、未確認事項をimplementationへ渡す。

### implementation

コード実装・修正を担当する。

### test

実装後のテストと不具合確認を担当する。

### security

実装後の詳細なセキュリティ監査を担当する。コードを変更せず、重要度・根拠・影響・修正方針を報告する。

### review

コードレビュー、設計上の問題、バグ、保守性を確認する。詳細なセキュリティ監査はsecurityを主担当とし、その結果を参照する。新たなセキュリティ懸念はsecurityへ戻す。

### release

最終テスト結果の確認と必要な再実行、README/CHANGELOGの更新、PR説明案、コミット候補、リリース前チェックを担当する。機能実装やテスト追加は各担当に戻す。公開操作はユーザーが明示的に指示した範囲に限る。

## Completion Criteria

タスク完了と判断してよい条件：

- 要求された機能が動作する
- 既存テストが成功する
- 必要なテストが追加されている
- security / review agent に未解決のCritical / Highがない
- その他の未解決事項・未確認事項が明示されている
- release agent が最終変更のテスト結果と必要なドキュメントを確認している
- 不要なファイルやデバッグコードが残っていない

## Reporting

最後に以下を簡潔に報告する。

- 変更した内容
- 変更したファイル
- 実行したテスト
- テスト結果
- security結果
- review結果
- release結果（準備の可否、PR説明案、コミット候補。実際の公開状況とは区別する）
- 残っている懸念点
