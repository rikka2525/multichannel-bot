# AI開発フロー

案件・アイデアを、検証済みのPRと人間による最終確認まで進める標準手順。
この文書は運用手順であり、エージェントを自動実行する設定ではない。

## 参照先と使い方

- プロジェクト共通ルールと完了基準：[`CLAUDE.md`](CLAUDE.md)
- 各担当の責務・制約： [`.claude/agents/`](.claude/agents/)
- 作業仕様： [TASK_TEMPLATE.md](.claude/tasks/TASK_TEMPLATE.md)
- 案件登録： [機能追加](.github/ISSUE_TEMPLATE/feature_request.md) / [不具合](.github/ISSUE_TEMPLATE/bug_report.md)
- PR説明： [pull_request_template.md](.github/pull_request_template.md)

担当の詳細な指示は参照先を正とし、この文書で再定義しない。案件ごとの明示的な指示・制約を最初に確認し、食い違いは着手前に整理する。
別案件へコピーするときは、CLAUDE.md・6担当の定義の有無と整合性を確認し、対象リポジトリのREADME、テスト手順、既定ブランチに合わせる。固有の機密情報や過去の検証結果を持ち込まない。

## 標準フロー

```text
案件 / アイデア
→ ChatGPTで要件整理
→ GitHub Issue
→ Claude Code：research → implementation → test → security → review → release
→ PR
→ 人間の最終確認
→ merge
```

1. **案件受付・要件整理**：目的、利用者、現状と期待動作、対象範囲、対象外、制約、受入条件を明文化する。前提と未確認事項を分け、実装を左右する不明点を解消する。
2. **Issue**：機能追加 / 不具合テンプレートで合意した内容を登録する。外部への登録操作は指示された範囲で行い、未許可なら本文案までとする。`/issue` はここで終了する。案件仕様は、別途 `/start <Issue番号>` を実行して開発に着手するときにTASK_TEMPLATEをコピーして作り、Issueと相互参照する。
3. **着手準備**：CLAUDE.md、担当定義、README、作業ツリー、現在のブランチを確認する。既存の未コミット変更を保護し、実装前に作業ブランチを用意する。既に案件用ブランチがあれば確認して利用する。
4. **6段階の開発**：下表の順に進める。親エージェントが要件、変更範囲・差分、前工程の結果、未解決事項を次の担当へ渡す。
5. **PR準備・作成**：releaseがPR本文案とコミット候補を用意する。明示的な指示がある場合のみ、その範囲でcommit・push・PR作成を行う。PRにはIssue、最終差分に対応した検証結果、残る制約を記載する。
6. **人間の最終確認**：差分、受入条件、検証結果、Security / Review、残課題、必要な移行・復旧手順を確認する。修正要求は該当工程に戻す。
7. **merge**：人間の確認と明示的なマージ指示を受けて行う。必要なCIが設定されている場合は成功を確認する。main同期・ブランチ削除・デプロイはそれぞれ指示された範囲で行う。マージと本番公開は区別する。

| 工程 | 次工程へ渡す内容 |
| --- | --- |
| research | 関連箇所、影響範囲、根拠、推奨方針、依存関係、懸念、検証観点 |
| implementation | 最小限の変更、変更理由、変更ファイル、検証してほしい箇所 |
| test | 実行コマンド、対象の変更状態、結果、失敗原因、未実行事項 |
| security | 専門監査の範囲、重要度別の指摘、根拠、修正方針、未確認事項 |
| review | security結果を踏まえた独立レビュー、重要度別の指摘、判定 |
| release | 最終差分と検証結果の照合、必要な文書、PR本文案、コミット候補、準備可否 |

## `/issue`：アイデアからIssueを作成

[`.claude/skills/issue/SKILL.md`](.claude/skills/issue/SKILL.md) を手動で呼び出す。

```text
/issue <機能アイデア または仕様資料>
```

既存コード・テンプレートを確認し、重大な曖昧さと重複がなければ `gh issue create` で1件登録する。重大な不明点、資料取得失敗、権限エラーでは登録せず確認点を報告する。送信結果が不明な場合も再送せず、GitHub上の状態を確認する。

Issue番号・URL・タイトル・要約を報告して終了する。Task作成・開発・commit・push・PR作成は行わない。内容を確認して開発を開始する場合は、ユーザーが別途 `/start <Issue番号>` を実行する。「下書きだけ」の依頼では登録しない。

## GitHub Actionsの自動テスト

- [Tests](.github/workflows/tests.yml)はPR作成・更新時と`main`へのpush時に実行する。
- READMEの動作確認環境に合わせたPython 3.12で、`python -m pip install -r requirements.txt`の後に`python -m unittest discover -s tests`を実行する。
- pipキャッシュは`requirements.txt`をキーの入力に使う。外部サービスの秘密情報は不要で、実接続の確認は含まない。
- PRテンプレートのTests欄にはローカルの検証結果とCI結果（実行URL、未実行ならその理由）を記録し、マージ前に最新のCI成功を確認する。

## 差し戻し条件

- Critical / Highが未解決なら完了不可。実装修正はimplementation、テスト追加・修正はtestへ戻す。
- 要件未達、テスト失敗、回帰、必要な検証不足も原因に応じて戻す。仕様自体が不明なら要件整理へ戻す。
- 修正後はtestで再検証し、security・reviewが影響範囲と指摘の解消を確認してからreleaseへ進む。release中・PRレビュー中の修正も同様。
- 新たなセキュリティ懸念はsecurityへ渡す。Medium / Lowは対応済みか、残す理由・影響・担当・対応予定を記録する。
- 最終変更に対応しない過去のテスト・監査結果を根拠に完了としない。環境不足は未確認として報告し、成功扱いしない。

## ブランチ / コミット命名規則

- ブランチ：`feat/<short-description>`、`fix/<short-description>`、`chore/<short-description>`。説明部分は英小文字とハイフンを基本とし、必要ならIssue番号を含める（例：`feat/123-admin-status`）。
- コミット候補：`feat: ...`、`fix: ...`、`test: ...`、`docs: ...`、`chore: ...`。変更目的が伝わる短い説明を付ける（例：`docs: AI開発フローの標準テンプレートを追加`）。
- 命名規則は操作の許可ではない。明示的な指示なしにgit add・commit・push・PR作成・merge・タグ付け・公開・デプロイを行わない。

## Definition of Done

以下は開発・PR準備完了の基準。マージ済み・公開済みを意味しない。

- [ ] 要件と受入条件を満たし、対象外の変更を混入させていない。
- [ ] 必要なテストを追加し、既存テストが成功している。最終変更に対応したコマンド・対象状態・結果を記録している。
- [ ] security / reviewに未解決のCritical / Highがない。確認範囲とその他の未解決・未確認事項を明記している。
- [ ] releaseが最終差分、検証結果、必要なドキュメントを確認し、準備可否を根拠付きで示している。
- [ ] 不要なファイル、秘密情報、デバッグコードを含まず、全変更ファイルとgit diffを確認している。
- [ ] PR説明案・コミット候補・残課題を提示し、実施済み操作と未実施操作を区別している。

案件指示で文書のみの変更についてアプリテスト不要とされている場合は、全文・リンク・ルール整合性・差分を確認し、アプリテストは「未実行：文書のみ、案件指示による」と記録する。コード・依存関係・実行設定に変更があれば、この扱いを適用しない。
PR後の完了には、人間の最終確認と指示されたマージの完了が別途必要。
